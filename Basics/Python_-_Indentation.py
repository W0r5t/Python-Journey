Python = 5 #We have Declared The Value of Python as 5
if Python == 5: #The if Statement creates a condition when the "==" is used,
                #We are using the == to say if the answer is equivalent to 
                #The Following value (5)
    print("This line is indented by 4 Spaces when we use the If Function") #Here the Print Statement is Indented By 
                                                                           #4 Statements since we're running an if function
